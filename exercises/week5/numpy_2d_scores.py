# ============================================
# 第 5 周 练习 2：班级成绩表（二维数组）📋
#
# 一维数组是一"行"数字，二维数组是一张"表"：
#         语文  数学  英语     ← 3 列（科目）
#   学生1  92   85   78
#   学生2  58   90   65       ← 5 行（学生）
#   学生3  85   45   88
#   学生4  76   76   76
#   学生5  95   88   92
#
# 新知识：二维数组索引 / axis 参数
# 旧知识：向量化运算 / 布尔索引 / 统计函数
# ============================================

import numpy as np

# 5 个学生 × 3 个科目
scores = np.array([
    [92, 85, 78],   # 学生1（语文 数学 英语）
    [58, 90, 65],   # 学生2
    [85, 45, 88],   # 学生3
    [76, 76, 76],   # 学生4
    [95, 88, 92],   # 学生5
])

# ---- TODO 1：看形状 ----
# print(f"形状：{scores.shape}")     # 应该是 (5, 3) = 5 行 3 列
# print(f"总共 {scores.size} 个成绩")

# ---- TODO 2：取某一行 / 某一列 ----
# 取学生1（第 1 行，索引 0）的全部成绩：
#   print(f"学生1的成绩：{scores[0]}")
# 取所有学生的语文成绩（第 1 列，索引 0）：
#   print(f"所有语文成绩：{scores[:, 0]}")
# 提示：", " 前的 : 表示"所有行"，所以 scores[:, 0] = 所有行的第 0 列

# ---- TODO 3：每个学生的平均分（按行算）----
# student_avg = scores.mean(axis=1)
# print(f"每个学生的平均分：{student_avg}")
# 提示：axis=1 → 横着压（压扁每一行）→ 得到"每行一个结果"

# ---- TODO 4：每门课的平均分（按列算）----
# subject_avg = scores.mean(axis=0)
# print(f"每门课的平均分（语文/数学/英语）：{subject_avg}")
# 提示：axis=0 → 竖着压（压扁每一列）→ 得到"每列一个结果"

# ---- TODO 5：全班所有成绩里及格的 ----
# passed = scores[scores >= 60]
# print(f"全班及格的成绩：{passed}")
# print(f"及格率：{len(passed) / scores.size:.1%}")

# ---- 挑战（可选）：把所有不及格的成绩替换成 0 ----
# np.where(条件, 满足时的值, 不满足时的值)
# fixed = np.where(scores < 60, 0, scores)
# print(f"不及格记 0 分：\n{fixed}")

# 示例输出：
#   形状：(5, 3)
#   学生1的成绩：[92 85 78]
#   所有语文成绩：[92 58 85 76 95]
#   每个学生的平均分：[85. 71. 72.67 76. 91.67]
#   每门课的平均分：[81.2 76.8 79.8]
#   及格率：86.7%
# ============================================
