# ============================================
# 第 2 周 练习：手机号整理器 📱
# 和"句子加工厂"同款知识点，没有新东西！
#
# 功能：输入一个带横线的手机号，帮你清洗、拆解、重组
#
# 用到的知识（全部学过）：
#   字符串：replace / split / 切片 [start:end] / len
#   旧知识：input / print / f-string / 变量
# ============================================

# ---- TODO 1：获取输入并清洗 ----
# raw = input("请输入手机号（带横线，如 138-1234-5678）：")
# 用 replace 把横线去掉：
#   cleaned = raw.replace("-", "")      ← 把 "-" 替换成空字符串 = 删掉它！
# 打印 f"清洗后：{cleaned}"
# 打印 f"号码长度：{len(cleaned)}"      ← 应该是 11 位

# ---- TODO 2：切片展示 ----
# 打印 f"区号（前 3 位）：{cleaned[:3]}"
# 打印 f"中间 4 位：{cleaned[3:7]}"     ← 切片 [3:7]：第 3~6 个字符
# 打印 f"尾号（后 4 位）：{cleaned[-4:]}"  ← 负数切片，从右边数

# ---- TODO 3：用 split 拆开看 ----
# parts = raw.split("-")               ← 按横线拆成 3 段
# 打印 f"拆分结果：{parts}"             ← ['138', '1234', '5678']

# ---- TODO 4：重组标准格式 ----
# 用切片 + 拼接，还原成标准格式（中间 4 位打码成 ****）：
# masked = f"{cleaned[:3]}-****-{cleaned[-4:]}"
# 打印 f"打码显示：{masked}"            ← 138-****-5678

# 示例输入：138-1234-5678
# 期望输出：
#   清洗后：13812345678
#   号码长度：11
#   区号（前 3 位）：138
#   中间 4 位：1234
#   尾号（后 4 位）：5678
#   拆分结果：['138', '1234', '5678']
#   打码显示：138-****-5678
# ============================================
