# ============================================
# 第 3 周 练习：迷你日记本 📔
#
# 功能：
#   1. 写日记（追加到文件，不覆盖之前的）
#   2. 看日记（读取文件全部内容）
#   3. 退出
#
# 新知识：文件读写（open / with / "a"追加 / "r"读取）
#        + FileNotFoundError（第 1 课知识联动）
# 旧知识：while / if-elif / input / print / strip
#
# 运行后会在【程序同目录】生成一个 日记.txt 文件
# ============================================

while True:
    print("\n1.写日记  2.看日记  3.退出")
    choice = input("请选择：")

    # ---- TODO 1：写日记（追加模式）----
    # 如果 choice == "1"：
    #   note = input("今天想记点什么：")
    #   with open("日记.txt", "a", encoding="utf-8") as f:
    #       f.write(note + "\n")      # 加 \n 换行
    #   print("已保存！")
    # 提示：用 "a"（追加）而不是 "w"（覆盖）——这样不会清掉以前的日记！

    # ---- TODO 2：看日记（读取模式）----
    # 如果 choice == "2"：
    #   try:
    #       with open("日记.txt", "r", encoding="utf-8") as f:
    #           for line in f:          # 一行一行读
    #               print(line.strip()) # strip() 去掉行尾换行
    #   except FileNotFoundError:       # 还没写过日记
    #       print("还没有日记，先写一篇吧！")
    # 提示：第一次运行选 2 时文件不存在 → FileNotFoundError → 接住提示！

    # ---- TODO 3：退出 ----
    # 如果 choice == "3" 或 choice == "q"：打印"再见！"然后 break

    # 加个兜底：否则打印"无效的选择，请重新输入"

# 测试步骤：
#   1. 选 1 写一篇 → 选 1 再写一篇（验证不覆盖！）
#   2. 选 2 查看 → 应该看到两篇都在
#   3. 关掉程序重开 → 选 2 → 日记还在！（持久化验证 ✅）
# ============================================
